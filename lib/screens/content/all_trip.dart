import 'package:flutter/material.dart';

class AllTripPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text(
          'All Trip Page',
          style: TextStyle(fontSize: 30),
        ),
      ),
    );
  }
}
