import 'package:admin/widget/date_time_picker.dart';
import 'package:flutter/material.dart';

class AddRoutesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: 
      DateTimePicker()
    );
  }
}
